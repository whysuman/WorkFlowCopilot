METRIC_KEYS = {
    "yield_pct",
    "metric_variance",
    "change_magnitude",
    "measurement_confidence",
    "affected_lot_count",
    "rework_rate",
    "time_window_hours",
}

# Example defaults (you asked for hard-coded defaults earlier).
# NOTE: In this app, we do NOT auto-fill these into inputs, to keep readiness honest.
